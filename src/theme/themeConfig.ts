import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: '#dadada',
  },
  components: {
    Layout: {
      headerBg: '#ffffff', 
      headerHeight: 64,
    },
  },
};

export default theme;